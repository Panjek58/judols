services:
  opensourcecasino:
    build: .
    ports:
      - "80:80"
    container_name: opensourcecasino-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - casino-network

networks:
  casino-network:
    driver: bridge
